# رفع مشکل جست‌وجوی «بهاره کربلایی» در گوگل

تاریخ: ۲۰۲۶/۰۹/۲۵ — این گزارش هم مشکل را توضیح می‌دهد و هم تغییراتی که انجام شد.

---

## ۱) خلاصه در یک نگاه

| موضوع | قبل | بعد |
|---|---|---|
| واژه‌های فارسی داخل HTML صفحه `/fa/` | **۳۵ واژه** | **۱٬۳۲۱ واژه** |
| کاراکترهای فارسی در متن قابل‌مشاهده | ۱۴۶ | ۵٬۱۱۱ |
| بخش‌های متنی فارسی (text node) | ۵ | ۲۲۳ |
| تیترهای فارسی (h1..h6) | ۰ | ۳۴ |
| نام «بهاره کربلایی» در متن قابل‌مشاهده | ۱ بار | ۴ بار (+ در H1 و اسکیما) |
| نام لاتین با املای دیگر (`Bahare Karbalaei`) | ۰ | در description، بیو، keywords و alternateName |
| حجم فایل صفحه فارسی | ۷۶ کیلوبایت | ۱۰۰ کیلوبایت |

با سرچ `site:baharehwebdesign.github.io/fa/` در گوگل هم می‌توانی بعد از ایندکس شدن، وجود صفحه را چک کنی.

---

## ۲) مشکل واقعاً کجا بود؟

### مشکل اصلی: صفحه فارسی فقط «قالب فارسی» بود، نه «متن فارسی»

سایت تو صفحه `/fa/` را دارد، عنوان فارسی، توضیحات فارسی، `lang="fa"` و `dir="rtl"` هم درست بود. اما **متن اصلی صفحه داخل فایل HTML وجود نداشت**. منوی سایت، تیترها، بخش «درباره من»، سوالات متداول، عنوان و توضیح دوره‌ها، عنوان ویدیوها و فوتر — همه، هنگام اجرا، توسط `script.js` و تابع `applyLang()` و با مکانیزم `data-i18n` و از داخل `APP_CONFIG.i18n.fa` **از جاوااسکریپت تزریق می‌شدند**.

نتیجه‌اش این بود که اگر یک موتور جست‌وجو صفحه را *قبل از اجرای جاوااسکریپت* بخواند، این‌ها را می‌بیند:

- یک `h1` با متن «code with Bahareh» (انگلیسی)،
- منو و تیترها و فوتر انگلیسی،
- در کل ۳۵ واژه فارسی در بدنه‌ی صفحه.

یعنی از دید گوگل، صفحه‌ای با برچسب زبان فارسی وجود داشت که **محتوایش انگلیسی بود**. برای یک عبارت فارسی مثل «بهاره کربلایی» گوگل به متن هم‌زبان نیاز دارد تا بفهمد موضوع صفحه چیست؛ تیتر و متای فارسی برای رتبه‌گرفتن روی کوئری فارسی کافی نیست. دقیقاً به همین دلیل کوئری لاتین (`bahareh karbalaei`) جواب می‌داد — چون در آن حالت متن انگلیسی صفحه با کوئری هم‌خوان بود — ولی «بهاره کربلایی» نه.

### مشکل دوم: نام خانوادگی در H1 نبود

`<h1>` صفحه فقط «کدنویسی با بهاره» بود؛ نام کامل (به‌ویژه «کربلایی») در مهم‌ترین تیتر صفحه وجود نداشت.

### مشکل سوم: اسکیما فقط یک‌طرفه بود

داده‌ی ساخت‌یافته (JSON-LD) `Person` با نام «Bahareh Karbalaei» و `alternateName` فارسی درست بود، ولی هیچ نود صفحه‌ای (WebPage/FAQPage) با `inLanguage: fa` و متن فارسی سوالات متداول وجود نداشت.

### مشکل چهارم (خارج از کد، ولی مهم): ایندکس و اعتبار

- «`site:`» نشان می‌دهد خیلی از صفحات حتی ایندکس نشده‌اند. اگر `/fa/` در Search Console ثبت نشده یا درخواست ایندکس نخورده باشد، هرچقدر هم محتوای فارسی بگذاری، در نتایج ظاهر نمی‌شود.
- برای جست‌وجوی **نام اشخاص**، گوگل به سیگنال اعتبار (اشاره و لینک از سایت‌های دیگر + پروفایل‌های اجتماعی هم‌نام) هم نگاه می‌کند. فقط اصلاح فنی، تضمین رتبه نیست.

---

## ۳) چه چیزی درست شد؟

### الف) متن واقعی داخل HTML صفحات زبان («bake» کردن)

`tools/generate_language_pages.py` بازنویسی شد. حالا موقع ساخت صفحات `/en/`, `/fa/`, `/it/`:

۱. همه‌ی عناصر `data-i18n`، `data-i18n-placeholder` و `data-i18n-aria-label` با متن ترجمه‌شده **داخل HTML نوشته می‌شوند** (۱۰۵ رشته در هر زبان).
۲. فهرست سوالات متداول (`#faqList`) به‌صورت استاتیک نوشته می‌شود (هر دو زبان و با همان مارک‌آپی که `renderFaq()` تولید می‌کند).
۳. بخش ویدیوهای یوتیوب (که قبلاً فقط برای فارسی و با جاوااسکریپت ساخته می‌شد) و شبکه‌ی ویدیوها استاتیک می‌شود.
۴. فهرست کامل دوره‌ها و کامپوننت‌ها (`#courseList`) به‌صورت استاتیک (عنوان + توضیح + برچسب‌ها) نوشته می‌شود. جاوااسکریپت بعداً همان‌جا کارت‌های تعاملی را می‌سازد، پس کاربر چیزی متفاوت نمی‌بیند.
۵. متن‌ها **بایت‌به‌بایت همان مقادیر `APP_CONFIG.i18n`** هستند؛ یعنی وقتی `script.js` صفحه را بومی‌سازی می‌کند، هیچ چیزی عوض نمی‌شود (صفحه دقیقاً مثل قبل دیده می‌شود) — فقط متن از قبل داخل HTML هست.

صحت این کار ماشینی تست شد: خروجی `renderFaq()`، `renderVideos()` و `courseVideosMarkup()` از روی خود `script.js` اجرا و با HTML تولیدشده مقایسه شد → **کاملاً یکسان**.

### ب) متن معرفی (بیو) در هر سه زبان

به بخش «درباره من» یک بلوک بیوی سه‌پاراگرافی اضافه شد؛ فارسی برای صفحه فارسی، انگلیسی برای صفحه انگلیسی/ریشه و ایتالیایی برای صفحه ایتالیایی. این متن نام کامل، شهر (اصفهان)، تخصص‌ها، دوره‌ها و راه‌های تماس را به‌صورت طبیعی توضیح می‌دهد (بدون تکرار مصنوعی کلیدواژه).

### ج) نام کامل در H1

داخل `<h1>` یک `span` با کلاس `u-sr` (مخصوص صفحه‌خوان‌ها) اضافه شد:

- فارسی: `کدنویسی با بهاره` + `کربلایی` → «کدنویسی با بهاره کربلایی»
- انگلیسی: «code with Bahareh Karbalaei»
- ایتالیایی: «programma con Bahareh Karbalaei»

این هم برای کاربران صفحه‌خوان مفید است (نام کامل برند را می‌شنوند) و هم نام کامل را در مهم‌ترین تیتر صفحه قرار می‌دهد. ظاهر صفحه **هیچ تغییری نمی‌کند**.

### د) داده‌ی ساخت‌یافته‌ی فارسی

به JSON-LD هر صفحه اضافه شد:

- نود `WebPage` با `inLanguage: fa|en|it`، `about` → شخص «بهاره کربلایی» و `isPartOf` → وب‌سایت.
- نود `FAQPage` با پرسش و پاسخ‌های همان زبان.

کلیدواژه‌های متای فارسی صفحه `/fa/` هم کمی طبیعی‌تر شد (اضافه شدن «بهاره کربلایی اصفهان» و شکل لاتین نام).

### ه) فایل جدید `tools/extract_app_config.js`

داده‌ها از `script.js` خوانده می‌شوند بدون اجرای کد سایت: این ابزار فقط آبجکت `APP_CONFIG` را از سورس استخراج و به JSON تبدیل می‌کند تا مبدل پایتونی بتواند از آن استفاده کند.

---

## ۴) فایل‌هایی که باید آپلود کنی

| فایل | تغییر |
|---|---|
| `index.html` | افزودن بلوک بیو + نام کامل در H1 (نسخه انگلیسی/ریشه) |
| `fa/index.html` | بازتولیدشده: متن فارسی واقعی، FAQ، دوره‌ها، بیو، اسکیمای فارسی |
| `en/index.html` | بازتولیدشده (تقریباً بدون تغییر محسوس + FAQ/دوره‌های استاتیک + اسکیما) |
| `it/index.html` | بازتولیدشده: حالا واقعاً ایتالیایی است (قبلاً `lang="it"` با متن انگلیسی بود!) |
| `sitemap.xml` | تاریخ `lastmod` صفحات زبان به‌روز شد |
| `tools/generate_language_pages.py` | بازنویسی‌شده (مبدل استاتیک‌ساز) |
| `tools/extract_app_config.js` | فایل جدید |
| `README.md` و `GITHUB_TREE.md` | مستندسازی روند جدید |

> `script.js` و `script.min.js` **دست نخورده‌اند**؛ پس نیازی به بیلد مجدد جاوااسکریپت یا تغییر `?v=` کش نیست.

اگر پروژه را لوکال داری، کافی است این را اجرا کنی:

```bash
npm run build            # یا فقط: npm run build:languages
git add .
git commit -m "SEO: bake localized page text into /en/, /fa/, /it/ (+ bio, FAQ, schema)"
git push
```

و بعد از هر تغییری در متن‌های `APP_CONFIG.i18n` یا سوالات متداول یا دوره‌ها، فقط `npm run build:languages` را بزن.

---

## ۴.b) املای لاتین نام در هر سه حالت (به‌روزرسانی)

سه عبارتی که می‌خواهی سایت با آن‌ها پیدا شود، الان این‌گونه پوشش داده شده‌اند:

| عبارت جست‌وجو | کجا آمده |
|---|---|
| **بهاره کربلایی** | H1 صفحه فارسی، متن بیو، تیترهای صفحه، سوالات متداول، عنوان/توضیح همه‌ی دوره‌ها، `title` و `description` و `keywords` صفحه `/fa/`، `name` و `alternateName` در اسکیمای Person |
| **Bahareh Karbalaei** | `title` صفحه اصلی/`/en/`/`/it/`، `description`، بیو، `name` در اسکیمای Person، `alt` تصویر پروفایل |
| **Bahare Karbalaei** (بدون h) | `title` صفحه فارسی، `description` هر چهار صفحه، پاراگراف بیو (انگلیسی و فارسی)، `keywords`، و `alternateName` در اسکیمای Person |
| «بهاره کربلائی» (با ئ) | `keywords` صفحه فارسی + `alternateName` اسکیما |

در ضمن «بهره کربلایی» (بدون الف) و «بهاره کربلایی اصفهان» و «کدنویسی با بهاره» هم در `alternateName` افزوده شدند؛ گوگل شکل‌های نزدیک نام را از همین `alternateName` به هم وصل می‌کند.

**یک تصمیم عمدی:** خود املای `Bahare Karbalaei` را کنار `Bahareh Karbalaei` در `title` نگذاشتم؛ دو عبارت تقریباً یکسان پشت‌سرهم در تیتر، شبیه «keyword stuffing» به‌نظر می‌رسد و می‌تواند کیفیت صفحه را در نظر گوگل پایین بیاورد. جایش در توضیحات (description)، بیو، کلیدواژه‌ها و اسکیما آمده که مکان‌های طبیعی‌تری هستند. اگر دوست داری در تیتر هم بیاید، یک خط تغییر است.

---

## ۴.c) آزمون «ظاهر عوض نشده؟» با مرورگر واقعی (Chromium)

برای این‌که فقط به حرف تکیه نکنی، نسخه‌ی قبلی و فعلی سایت را کنار هم روی دو پورت سرو کردم، هر دو را با Chromium واقعی رندر کردم و مقایسه کردم:

| بررسی | نتیجه |
|---|---|
| متن رندرشده صفحه فارسی (پس از اجرای کامل JS) | ۳٬۱۳۳ → ۴٬۱۹۲ کاراکتر؛ **تنها اختلاف، پاراگراف‌های بیو هستند** (diff کلمه‌به‌کلمه: فقط یک بلوک «insert») |
| متن رندرشده صفحه اصلی / `/en/` / `/it/` | همان نتیجه: فقط اضافه‌شدن بیو، هیچ حذف یا تغییری در متن‌های موجود |
| ارتفاع و اندازه‌ی همه‌ی بخش‌ها (hero، دوره‌ها، FAQ، فرم، فوتر، سبد خرید) | **صفر پیکسل اختلاف**؛ فقط `#about` از ۵۴۸px به ۹۳۶px رسید (چون بیو اضافه شده) |
| جعبه‌ی `<h1>` | `x=87, y=195, 1192×125.25` — **دقیقاً یکسان** در قبل و بعد (تصویرش هم گرفته و چشمی تأیید شد) |
| تفاوت پیکسلی صفحه‌ی hero | ۱.۴٪ که همه‌اش در نوار انیمیشن پس‌زمینه است؛ خودِ همان صفحه در دو بار لود متوالی ۴.۳٪ تفاوت نشان می‌دهد (یعنی نویز انیمیشن، نه تغییر چیدمان) |

پس: **ظاهر و چیدمان دست‌نخورده است؛ تنها تغییر دیده‌شدنی، اضافه‌شدن متن بیو در «درباره من» است.**

متن‌های داخلی هم بایت‌به‌بایت با دیکشنری `APP_CONFIG.i18n` یکسان‌اند (۹۳ تا ۱۰۵ رشته در هر زبان، صفر اختلاف)؛ یعنی حتی یک برچسب، دکمه یا عنوان عوض نشده.

---

## ۵) کارهایی که بعد از آپلود باید انجام بدهی (بدون این‌ها نتیجه نمی‌گیری)

۱. **Google Search Console** (`search.google.com/search-console`) → اگر سایت اضافه نشده، اضافه کن (فایل‌های `google…html` و `BingSiteAuth.xml` از قبل در ریپو هست، پس مالکیت تأیید می‌شود). این فایل‌ها را حذف نکن.
۲. در بخش **URL Inspection** این آدرس را وارد کن و **Request Indexing** بزن:
   - `https://baharehwebdesign.github.io/fa/`
   - `https://baharehwebdesign.github.io/`
   - و اگر شد `/en/` و `/it/`
۳. در Search Console → **Sitemaps** آدرس `sitemap.xml` را ثبت کن.
۴. **لینک‌سازی (مهم‌ترین کار برای اسم اشخاص):** در جاهایی که به‌نام خودت اشاره می‌کنی، با انکر «بهاره کربلایی» به سایت لینک بده:
   - README پروفایل گیت‌هاب (پروفایل `codewithbahareh` و اکانت `baharehwebdesign`)،
   - بایو اینستاگرام و دسکریپشن کانال یوتیوب (لینک سایت + نام فارسی)،
   - لینکدین، تلگرام، پینترست، مِدیوم/ویرگول (یک مقاله کوتاه «چه‌طور HTML یاد بگیریم» با امضای نام + لینک)،
   - سایت‌های فریلنسری فارسی و دایرکتوری‌ها (پونیشا، کارلنسر، پارس‌کدرز، سایت‌سازها) با پروفایل عمومی،
   - پاسخ دادن به سوال‌های مرتبط در انجمن‌ها/کامنت یوتیوب با لینک سایت.
   هرچه سایت‌های معتبرتر و متنوع‌تر به‌نام «بهاره کربلایی» لینک بدهند، احتمال نمایش در جست‌وجوی نام بیشتر می‌شود.
۵. **Google Business Profile** (اختیاری، اگر خدمات محلی اصفهان می‌دهی) می‌تواند به شناخته‌شدن شخص/کسب‌وکار کمک کند.
۶. **زمان:** ایندکس مجدد معمولاً چند روز تا چند هفته طول می‌کشد. انتظار نداشته باش بلافاصله بعد از `push` نتیجه ظاهر شود.

---

## ۶) چطور مطمئن شویم درست کار می‌کند؟

```bash
# ۱) متن فارسی باید داخل سورس صفحه باشد
curl -s https://baharehwebdesign.github.io/fa/ | grep -c "بهاره کربلایی"     # انتظار: بیشتر از ۰

# ۲) بررسی دوم: چند واژه فارسی در سورس هست
curl -s https://baharehwebdesign.github.io/fa/ | grep -oP '[\x{0600}-\x{06FF}]+' | wc -l   # انتظار: بیش از ۱۰۰۰

# ۳) اعتبارسنجی داده‌ی ساخت‌یافته
#    https://search.google.com/test/rich-results  → آدرس /fa/ را بده
```

در مرورگر هم `view-source:` صفحه فارسی را باز کن؛ باید تیترها، سوالات متداول و عنوان دوره‌ها را فارسی ببینی.

---

## ۷) نکته‌ی مهم درباره‌ی «مشکل کجا بود»

خلاصه‌ی یک‌خطی: **مشکل در نبود متن فارسی در سورس HTML بود، نه در تیتر و متا و نه در ایندکس شدن؛** سایت به‌شکلی ساخته شده بود که محتوای هر زبان را فقط با جاوااسکریپت می‌ساخت، و گوگل برای کوئری فارسی به متن هم‌زبان داخل صفحه نیاز دارد. حالا محتوای هر زبان موقع ساخت داخل HTML نوشته می‌شود و ظاهر سایت برای کاربران هیچ تغییری نکرده است.


# 📈 گزارش تغییرات سئو — سایت Code with Bahareh


## ✅ چه تغییراتی در هر فایل انجام شد

| فایل | تغییرات |
|---|---|
| `index.html` | عنوان/توضیح/keywords جدید با نام کامل (EN) • متا geo برای اصفهان • **JSON-LD کاملاً بازنویسی شد**: موجودیت Person با نام «Bahareh Karbalaei» + نام‌های جایگزین فارسی («بهاره کربلایی»...) + آدرس اصفهان • افزودن **ProfessionalService** (برای رتبه‌بندی محلی «در اصفهان») • WebSite با نام‌های جایگزین فارسی |
| `fa/index.html` | عنوان: **«بهاره کربلایی \| طراحی صفحات وب — وب دولوپر در اصفهان»** • توضیح و keywords فارسی • متن About فارسی با نام کامل (قابل‌کراول حتی بدون جاوااسکریپت) • فیکس باگ og:locale |
| `en/index.html` | عنوان: **«Bahareh Karbalaei — Web Developer & Web Designer \| Code with Bahareh»** • توضیح/keywords انگلیسی با Isfahan |
| `it/index.html` | عنوان: **«Bahareh Karbalaei — Sviluppatrice Web \| Code with Bahareh»** • توضیح/keywords ایتالیایی • فیکس باگ og:locale |
| `script.js` + `script.min.js` | متن about_p1 در هر سه زبان با نام کامل و کلمات کلیدی به‌روز شد (نسخه‌ی minified هم با terser بازسازی شد) |
| `sitemap.xml` | تاریخ lastmod به 2026-09-03 به‌روز شد |
| `tools/generate_language_pages.py` | مولد صفحات زبانی به‌روز شد تا همه‌ی این بهینه‌سازی‌ها به‌صورت خودکار روی هر سه زبان اعمال (و اکنون باگ og:locale:alternate را هم اصلاح می‌کند) |

### نام‌های جایگزینی که گوگل حالا می‌شناسد (در داده‌های ساخت‌یافته):
بهاره کربلایی · بهاره کربلایی وب دولوپر · بهاره کربلایی طراح وب · کدنویسی با بهاره · Code with Bahareh · Bahareh Web Design

---

## 🚀 روش انتشار (۵ دقیقه)

1. فایل‌های داخل پوشه‌ی `bahareh-seo` را روی فایل‌های ریپوتان **کپی و جایگزین** کنید (مسیر هر فایل دقیقاً مثل ریپو حفظ شده).
2. فایل باقی‌مانده‌ی قدیمی را حذف کنید (تکراری و مضر برای سئو):
   ```bash
   git rm it/111index.html
   ```
   (اختیاری: فایل بدون‌کاربرد `img/hi` هم قابل‌حذف است)
3. کامیت و پوش:
   ```bash
   git add -A
   git commit -m "SEO: add full name/keywords/localized metadata + fixed locale alternates"
   git push
   ```

---

## 🎯 بعد از انتشار (خیلی مهم — تاثیر را از هفته‌ها به روزها می‌رساند)

1. **Google Search Console** → بخش Sitemaps → آدرس `https://baharehwebdesign.github.io/sitemap.xml` را دوباره ثبت کنید.
2. در GSC روی **URL Inspection** کلیک کنید و برای این ۴ آدرس دکمه‌ی **Request Indexing** بزنید:
   - `https://baharehwebdesign.github.io/`
   - `https://baharehwebdesign.github.io/en/`
   - `https://baharehwebdesign.github.io/fa/`
   - `https://baharehwebdesign.github.io/it/`
3. **Bing Webmaster Tools** (شما فایل BingSiteAuth.xml را دارید) → سایت‌مپ را دوباره ارسال کنید.
4. در اینستاگرام/یوتیوب/تلگرام خود لینک سایت را بگذارید — این لینک‌های برگشتی (بک‌لینک) برای رتبه‌بندی نام‌تان طلایی‌اند.

---

## ⏱️ انتظار واقع‌بینانه (لطفاً بخوانید)

- عبارات **نام‌شخصی** («بهاره کربلایی»، «Bahareh Karbalaei»، «بهاره کربلایی وب دولوپر»): رقابت پایین است و معمولاً **طی یک تا چهار هفته** بعد از ایندکس مجدد در صفحه‌ی اول ظاهر می‌شوید. این سریع‌ترین نتیجه است.
- عبارات **ترکیبی** مثل «طراحی صفحه وب در اصفهان»، «web designer Isfahan»: چند ماه زمان می‌برد و به محتوای بیشتر و بک‌لینک نیاز دارد.
- عبارات **بسیار عمومی** مثل «طراحی صفحات وب» یا «web design»: بسیار رقابتی‌اند؛ سایت‌های قدیمی و بزرگ رتبه دارند. این عبارات را هدف بزنید ولی انتظار نتیجه‌ی فوری نداشته باشید. مقالات بلاگ فارسی (مثلاً «چگونه یک صفحه‌ی وب طراحی کنیم») بهترین سلاح برای این دسته است.

راه‌های تقویت ادامه‌دار: ① صفحه‌های اختصاصی «طراحی سایت در اصفهان» به فارسی ② ثبت در Google Business Profile ③ مقاله‌ی بلاگ با کلمات کلیدی فارسی ④ لینک‌سازی از دی‌و‌تِن/Dev.to و LinkedIn.

---

## ⚠️ نکته‌های فنی برای مراجعات بعدی

- برای هر تغییر در متن‌های سایت، فقط `index.html` و `script.js` را ویرایش کنید و بعد این‌ها را بزنید تا ۴ صفحه و فایل minified ساخته شوند: `npm run build`
- صفحه‌ی ریشه (`/`) و `/en/` انگلیسی‌اند و نقش x-default را دارند — کانوایکال درست تنظیم شده‌اند.
- اگر روزی دامنه‌ی اختصاصی خریدید (توصیه می‌شود)، فقط مقدار ORIGIN بالای اسکریپت‌ی تولیدکننده و آدرس‌های canonical/hreflang/sitemap را به‌روز کنید.

موفق باشید بهاره! 🌷
